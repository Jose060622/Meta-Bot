import { GraphTokenProvider } from "../utils/login";
interface IAadAppInfo {
    displayName: string;
    id: string;
    appId: string;
    createdDateTime: string;
    deletedDateTime: string;
}
export declare class AadManager {
    private static instance;
    private static axios?;
    private static provider?;
    private constructor();
    static init(provider?: GraphTokenProvider): Promise<AadManager>;
    searchAliveAadApps(prefix: string, offsetHour?: number): Promise<IAadAppInfo[]>;
    searchDeletedAadApps(prefix: string, offsetHour?: number): Promise<IAadAppInfo[]>;
    searchAadApps(contain: string, offsetHour?: number): Promise<IAadAppInfo[]>;
    deleteAadAppById(id: string, retryTimes?: number): Promise<boolean>;
    deleteAadApps(contain: string, offsetHour?: number, retryTimes?: number): Promise<void>;
}
export {};
//# sourceMappingURL=aadManager.d.ts.map